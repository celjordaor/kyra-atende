// Arquivo mantido por compatibilidade — autenticação migrada para SupabaseAuthGuard
// que usa supabase.auth.getUser() ao invés de verificação local de JWT
export { AuthUser } from './supabase.guard';
